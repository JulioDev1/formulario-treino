import { Routes } from '@angular/router';
import { FormComponents } from './components/form/form.component';

export const routes: Routes = [
    {
        path:'form',
        component:FormComponents
    }
];
